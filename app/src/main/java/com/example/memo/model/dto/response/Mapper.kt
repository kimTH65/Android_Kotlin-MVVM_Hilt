package com.example.memo.model.dto.response

import android.graphics.Movie
import com.example.memo.model.dto.MovieResponse

object Mapper {
    fun mapperSource(response: List<MovieResponse>?) : List<MovieResponse>?{
        response.Movie
    }

    fun List<MovieResponse>.toDomain()
}